__pycache__/
*.pyc
.weights.json
.retractions.json
.entities.json
# 块向量缓存（云端/本地 embedding 档建库时算一次、缓存在本机）——
# 它带着语料内容的指纹，跟上面几个 sidecar 一样不该被谁误提交进来
.embed_cache.json
init_state.json
threads.jsonl

# 用户自己的产出物绝不该进这个仓库
CLAUDE.md
AGENTS.md
memory/
mcp-config.json
问卷prompt.txt
# generic 档（自建前端）的产出：人格文件叫 persona.md，随货还多一份契约副本。
# 契约那条**必须锚在根目录**——不锚的话会连仓库自己的 docs/注入契约.md 一起忽略掉
/persona.md
/注入契约.md
# 换客户端档时旧人格文件退役成的 .bak，同样是用户的私人内容
*.bak

# 部署密钥与私人记忆归档同样禁止进入 Git 历史
.env
*.env
*.key
*.pem
*.tar
*.tar.gz
*.zip
